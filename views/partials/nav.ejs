  <% if (!user) { %>
    <script defer src="https://accounts.google.com/gsi/client"></script>
  <% } %>
</head>
<body>
  <nav>
    <div class="navbar">
      <% if (user?.role >= 100) { %> 
        <a href="/portal" <%- title === 'Home' ? 'class="active"' : '' %>>Door2Door</a>
        <a href="/portal" <%- title === 'Takeoffs' ? 'class="active"' : '' %>>Takeoffs</a>
        <a href="/portal" <%- title === 'Create Takeoffs' ? 'class="active"' : '' %>>Create Takeoff</a>
      <% }%>
      <% if (user?.role >= 200) { %>
          <a href="/portal" <%- title === 'Assets' ? 'class="active"' : '' %>>Assets</a>
      <% } %> 
        </div>
    </div>
    <div>
      <% if (user) { %>
        <a href="/auth/logout">Logout</a>
      <% } else { %>
        <div 
          id="g_id_onload"
          data-client_id="<%= googleClientID %>"
          data-login_uri="/auth/google"
          data-auto_prompt="false"
        ></div>
        <div 
          class="g_id_signin right"
          data-type="standard"
          data-size="large"
          data-theme="outline"
          data-text="sign_in_with"
          data-shape="rectangular"
          data-logo_alignment="left"
        ></div>
      <% } %>
    </div>
  </nav>
